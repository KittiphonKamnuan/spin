name: Deploy to DirectAdmin
on:
  push:
    branches: [ main ]

jobs:
  FTP-Deploy-Action:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: kamnuantech.com
        username: spinkitti@kamnuantech.com
        password: ${{ secrets.FTP_PASSWORD }}
        protocol: ftps   # เพิ่ม protocol
        port: 21        # เพิ่ม port
        server-dir: /home/kamnuant/domains/kamnuantech.com/spin.kamnuantech.com/public_html/
        dangerous-clean-slate: true   # ล้างไฟล์เก่าก่อน upload
        timeout: 60000   # เพิ่มเวลา timeout
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.env
          **/package-lock.json
          **/.htpasswd/**
          **/private_html/**
          **/cgi-bin/**